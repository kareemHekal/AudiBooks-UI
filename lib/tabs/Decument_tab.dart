import 'package:flutter/material.dart';

class DecumentTab extends StatelessWidget {
  const DecumentTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.cyan,
    );
  }
}
